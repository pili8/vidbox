# VidBox 完成品规划与执行文档（交接给执行 AI）

> 本文档是唯一权威需求来源。执行目标：**下一个版本（v0.1.0）直接交付完成品**，覆盖本文档全部需求，不再小步迭代。
>
> 交接日期：2026-08-14 ｜ 当前已发布：v0.0.4 ｜ 目标版本：v0.1.0（一次性交付）

---

## 0. 给执行 AI 的总纲（必读）

1. **先通读本文档全文**，再读现有代码。现有代码是可用的基础，不是要推翻的重写对象。
2. **本文档标注「不可违背」的约束，任何情况下不得更改**。它们是经过多轮真实样本验证和用户确认的决策，改动会破坏数据正确性。
3. 一次性完成所有需求后，统一提交、打 tag `v0.1.0`、推送触发 GitHub Actions 构建发布。**不要拆成多个中间版本**。
4. 遇到本文档未覆盖的细节，按「最简可行」原则自行决策并在 PR/commit 说明，不要停下来询问。
5. 本机（用户 Mac）没有 Flutter 环境，**不要尝试本地编译**。代码正确性靠仔细审查 + 云端构建验证。构建失败看 Actions 日志修复。

---

## 1. 项目背景与定位

- **产品**：VidBox，个人自用 Android APP，不上架应用商店。
- **场景**：管理手机本地几百至上千个从抖音下载的视频与图片。核心体验是"全屏刷着顺手整理"——像刷抖音一样浏览，顺手完成星标/分类/删除。
- **用户**：仅一位（仓库所有者），Android 手机，文件在普通目录（非系统相册，可能有 `.xxx` 隐藏目录）。
- **GitHub**：`pili8/vidbox`（Flutter 版，本仓库）。旧 Kotlin 版在 `pili8/vidbox_api`，与本仓库无关，勿动。

## 2. 文件命名规范（核心输入，不可违背）

抖音第三方工具下载的文件采用结构化命名：

```
dy<N>_<作者>_<类型>_<标题>_<14位时间戳>_<序号>.<扩展名>
```

真实样本：`dy1_全球超模_视频_秀场花絮潮汕服博会@申雪儿_20260812000107_1.mp4`

| 字段 | 说明 | 用途 |
|------|------|------|
| `dy<N>` 的 N | **星级编码：1=基线（未收藏），2~5=收藏星级**。改星=改这个数字，**禁止添加 `[★n]` 等任何前缀**，保持文件名原风格 | 收藏/重要度 |
| 作者 | 抖音作者名，**可能含下划线、emoji**（如 `美女推荐官📷`） | 分组主键 |
| 类型 | `视频` 或 `图集` | 区分单视频/图片组 |
| 标题 | 作品描述，可含 `#话题` `@提及`，**可为空**（连续 `_`） | 展示 |
| 时间戳 | 14 位 YYYYMMDDHHMMSS | 同批次判断、图集排序 |
| 序号 | 图集第几张 | 图集内排序 |

**解析策略（已验证，照抄实现，勿改）**：
1. 剥扩展名 → 剥尾部 `_序号` → 剥尾部 `_14位时间戳`（尾部反推）
2. 剩余部分用「视频/图集」类型词定位边界：类型词之前全为作者（抗作者名含下划线），之后为标题
3. 剥离遗留的方括号前缀（如旧版 `[★3]`，防御性兼容，但新代码绝不生成）

**边界情况**（真实样本已出现，必须全部处理）：
- 空标题：`dy1_是香墨呀_视频__20260813125122_1.mp4` → 标题=""，仍正确归组
- 裸标题文件：无 `dy` 结构、含空格/emoji/`(1)(2)` 重名后缀 → 归「待整理」组
- 纯数字命名（`VID_20240101_120000.mp4`）→ 同归「待整理」
- 真实样本目录（Mac 上，仅用于逻辑参考）：`/Users/gm/AI/二号回收站/dou`（22 文件，13 个结构化 + 9 个裸标题）

## 3. 核心设计约束（不可违背）

| 约束 | 内容 | 原因 |
|------|------|------|
| 文件系统即真相 | 分类=文件夹、星级=文件名数字、分组=文件名解析结果。**App 内任何状态不得只存在于 App 私有存储**（SQLite 索引和缩略图缓存除外——它们只是缓存、可随时重建） | 换机/重装零丢失 |
| 星级编码 | 只改 `dy` 后的数字（1~5），文件名其余部分一字不动 | 用户明确要求保持原风格 |
| 确定性分组 | 只按解析出的字段（作者/类型/序号/时间戳）分组，**禁止模糊相似度算法** | 确定性>聪明 |
| 删除安全 | 删除一律先进 `.trash` 隐藏目录（在文件所在目录下创建），物理删除只在回收站手动清空时发生 | 防误删 |
| 技术栈 | Flutter（UI+逻辑）+ Kotlin MethodChannel（文件操作），通道名 `vidbox/file` | 现有基础 |
| 构建方式 | GitHub Actions 云端构建，push tag `vX.Y.Z` 自动构建 APK + 创建 Release | 本地无环境 |

## 4. 现状盘点（v0.0.4 已实现，可用的基础）

**已实现，保留并可作为基础**：
- 文件名解析器（`lib/core/filename_parser.dart`）——含类型词定位作者边界，13/13 真实样本通过
- 分组引擎（`lib/core/grouper.dart`）——作者组 + 图集组 + 待整理
- Kotlin 文件通道（`MainActivity.kt`）——扫描（递归）、移动、重命名、删除到 .trash、缩略图、回收站管理、子目录列表、文件元数据（mtime/size）
- 全屏流（`feed_page.dart`）——上下滑、双击星标（dy1↔dy5）、底部操作条（星级 1~5/移动/删除）
- 网格模式（`grid_page.dart`）——按作者分组折叠、长按多选、批量删除/加星/移动、视频首帧缩略图
- 回收站（`trash_page.dart`）——查看/恢复/手动清空
- 待整理改名归位（home_page）——点击文件名重命名，重新解析归组
- 设置持久化（`settings_service.dart`）——多扫描目录，shared_preferences
- SQLite 索引（`index_service.dart`）——增量扫描（path+mtime+size）+ 缩略图磁盘缓存（md5 文件名）
- 权限：`MANAGE_EXTERNAL_STORAGE`，启动引导授权

**已知缺陷（必须在本版修复）**：
1. 网格点进全屏、改星标后返回，网格不刷新（数据同步缺失）
2. 批量操作无错误处理：中途失败无提示、无部分完成报告
3. Kotlin 通道全部在主线程执行，千级文件扫描/缩略图提取会卡 UI 甚至 ANR
4. 所有操作失败均静默（返回 null/false 无 UI 提示）
5. 待整理文件只能逐个改名，几百个时不可用
6. 没有任何单元测试

## 5. 完成品需求清单（v0.1.0 全部实现）

### 5.1 浏览体验（全屏流）

- [ ] 上下滑切换、双击星标（dy1↔dy5）、底部操作条（星级 1~5 选择/移动/删除/重命名）——已有，补重命名
- [ ] **重命名**：全屏流内可对当前文件重命名（弹对话框），改完即时重新解析、刷新当前项信息
- [ ] **位置指示**：右下角或顶部显示「第 N / 总数」
- [ ] **已看标记**：滑过的项记为已看（仅内存/索引缓存，不写文件名），网格中已看项加视觉标记（如角标或降低透明度），提供「清除已看记录」入口
- [ ] **图集整体浏览**：图集组在全屏流中作为一个整体项——左右滑切换组内图片，上下滑切换到下一项；底部显示图集信息（第 x/N 张）
- [ ] 单击暂停/继续播放（对齐抖音习惯）
- [ ] 图片项单击切换「适应屏幕/填满屏幕」两种缩放模式

### 5.2 网格模式

- [ ] 按作者分组折叠、长按多选、批量删除/加星/移动——已有
- [ ] **图集在网格中显示为一个聚合项**：封面图 + 角标「N 图」，点开进全屏流图集模式
- [ ] **多选增强**：组头有「选整组」按钮；全选/反选
- [ ] **筛选栏**：全部 / 已收藏（星级≥2）/ 待整理 / 图集；可与作者分组叠加
- [ ] **搜索**：标题栏搜索框，匹配作者名或标题（子串匹配即可，实时过滤）
- [ ] **排序**：时间戳 新→旧 / 旧→新、星级 高→低，网格设置入口切换
- [ ] 网格缩略图角标显示视频时长（如 `0:15`，格式 mm:ss，≥1h 用 h:mm:ss）

### 5.3 首页（分组总览）

- [ ] 作者列表（条数、图集组数）——已有
- [ ] **新增「已收藏」入口**：星级 ≥2 的所有项，进入全屏流或网格
- [ ] **新增「图集」入口**：所有图集组列表
- [ ] 作者条目显示：该作者最新一项的缩略图 + 总数 + 已收藏数
- [ ] 扫描改为**启动时自动增量扫描**（索引在则秒开），保留手动刷新入口；App 回到前台时自动增量扫描
- [ ] 首次全量扫描时显示进度（已扫描文件数）

### 5.4 回收站

- [ ] 查看/恢复/手动清空——已有
- [ ] **条目显示删除时间**（进 .trash 时用文件 mtime 即可）
- [ ] **N 天自动清理**：App 启动时清理 .trash 中 mtime 超过 30 天的文件（N 做成设置项，默认 30）
- [ ] 恢复/清空操作有 SnackBar 结果反馈

### 5.5 待整理工作流

- [ ] 待整理列表支持**批量改名**：选中多个 → 输入作者名（和可选标题前缀）→ 生成 `dy1_<作者>_视频_<原标题或时间戳>_<时间戳>_<序号>.mp4` 格式批量重命名
- [ ] 待整理项在全屏流中也可触发重命名对话框（同 5.1）
- [ ] 重命名失败（非法字符/超长）给出明确错误提示，不静默

### 5.6 稳定性与性能（必须完成）

- [ ] **Kotlin 通道异步化**：扫描、缩略图提取放到后台线程（Kotlin 协程或简单 Thread + Handler），主线程只回传结果
- [ ] **批量操作结果反馈**：批量删除/移动/加星完成后 SnackBar 汇总「成功 N 个，失败 M 个」；失败的列出原因（如目标已有同名文件）
- [ ] **跨页面数据同步**：建立单一数据源（推荐：HomePage 持有可变 MediaItem 列表 + 全局刷新回调，或引入简单 ChangeNotifier）；全屏/网格任何修改，返回后对应页面立即反映
- [ ] **文件名长度校验**：重命名前检查新名字节数 ≤255（UTF-8），超长截断标题并提示
- [ ] 关键路径日志：扫描、重命名、移动、删除失败时 `debugPrint` 原因（release 构建自动剔除）

### 5.7 测试（最低要求）

- [ ] `filename_parser_test.dart`：覆盖——标准命名、空标题、作者含下划线、作者含 emoji、裸标题、纯数字、旧版 `[★n]` 前缀防御剥离、buildStarredFilename 各星级、`dy0`/`dy9` 越界 clamp
- [ ] `grouper_test.dart`：作者分组、图集聚组（同标题/空标题退化时间戳）、待整理归组、组内排序
- [ ] GitHub Actions 构建流程中加 `flutter test` 步骤（在 build 之前）

### 5.8 版本与发布

- [ ] `pubspec.yaml` version 改为 `0.1.0+10`
- [ ] 全部完成后一次提交（或少量逻辑提交），打 tag `v0.1.0` 推送，确认 Actions 构建成功、Release 发布
- [ ] README 更新：新功能说明、v0.1.0 变更列表

## 6. 明确不做（non-goals，勿画蛇添足）

- 视频编辑、云同步、账号体系、播放历史记录
- 一个文件属于多个分类（物理限制，接受）
- 模糊相似度分组、AI 内容识别
- iOS 支持（虽然用 Flutter，但当前只构建 Android）
- 任何形式的自动化上架/商店合规处理

## 7. 架构与代码组织指引

```
lib/
  main.dart
  models/media_item.dart        # 数据模型（已存在）
  core/
    filename_parser.dart        # 解析器（已存在，加测试）
    grouper.dart                # 分组引擎（已存在，加测试）
  services/
    file_service.dart           # MethodChannel 封装（已存在）
    index_service.dart          # SQLite 索引 + 缩略图缓存（已存在）
    settings_service.dart       # 设置持久化（已存在，可扩展键）
    media_store.dart            # 【新增】单一数据源：持有 items，暴露变更通知
  ui/
    home_page.dart              # 首页总览（扩展：已收藏/图集入口、自动扫描）
    feed_page.dart              # 全屏流（扩展：图集模式、重命名、进度、已看）
    grid_page.dart              # 网格（扩展：筛选、搜索、排序、时长角标、多选增强）
    trash_page.dart             # 回收站（扩展：删除时间、自动清理）
    move_dialog.dart            # 移动对话框（已存在）
    widgets/                    # 【新增】共享组件（星级行、缩略图、角标等，从现有页面抽取）
```

要点：
- **`media_store.dart` 是本版新增的最重要结构**：把「数据 + 修改操作 + 通知」集中，全屏/网格/首页都从它读写，解决跨页面同步问题。用 `ChangeNotifier` 即可，**不要引入 riverpod/bloc 等状态框架**（依赖最小化）。
- 缩略图组件从 feed/grid 抽取共享，统一走 `IndexService.getThumbnail`。
- 已看记录存 `shared_preferences`（key: `seen_paths`，存 path 集合的 JSON），属可丢弃缓存。

## 8. 执行步骤（建议顺序）

1. **通读现有代码**（lib/ 全部 + MainActivity.kt + build.yml），理解现有实现。
2. **写单元测试**（5.7），先让现有解析器/分组引擎的测试通过——这是安全网。
3. **建 `media_store.dart`** 单一数据源，把三个页面改造为从它读写（修复跨页面同步）。
4. **Kotlin 异步化** + 批量操作结果反馈 + 错误提示（5.6）。
5. **图集整体浏览**（全屏左右滑 + 网格聚合项）——工作量最大的一块，放在数据层稳定后做。
6. 首页入口扩展（已收藏/图集/作者缩略图/自动扫描/进度）。
7. 网格筛选/搜索/排序/时长角标/多选增强。
8. 待整理批量改名 + 重命名校验。
9. 回收站扩展（删除时间/自动清理）。
10. 全屏流细节（位置指示/已看/单击暂停/图片缩放/重命名）。
11. Actions 加 `flutter test`；README 更新；版本号 `0.1.0+10`；提交、打 tag、推送、确认构建与 Release。
12. 若构建失败：读 Actions 日志修复，重复直到成功。**tag 已发布后如需修复，递增 patch 位（v0.1.1）重打，不要强推覆盖已发布的 tag。**

## 9. 验收标准（完成品的定义）

- [ ] 全部 5.x 清单项实现，4 节已知缺陷全部修复
- [ ] 单元测试全绿，`flutter test` 进入 CI 流程
- [ ] GitHub Actions 构建成功，v0.1.0 Release 发布，APK 可安装启动
- [ ] 用以下场景人工自检逻辑（可在代码审查层面推演）：
  - 扫描含结构化+裸标题+图集的目录 → 分组正确、待整理正确
  - 全屏双击 → 文件名 dy1→dy5，返回网格立即显示星标
  - 网格批量移动到子目录 → 重新扫描后文件仍在（递归扫描覆盖）
  - 删除 → .trash 可见可恢复
  - 图集在全屏左右滑、网格聚合显示
  - 待整理批量改名 → 重新解析归组成功

---

## 附：环境速查

- 仓库：https://github.com/pili8/vidbox（main 分支）
- 工作目录（用户 Mac）：`/Users/gm/AI/VidBox`
- 构建：push tag `v*` 触发 `.github/workflows/build.yml` → APK 附到 Release
- 依赖见 `pubspec.yaml`；新增依赖保持最小化，禁止引入状态管理框架
- 用户测试反馈渠道：对话中口头反馈 + 版本号定位
